# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native build outputs (android/ is generated by `expo prebuild`).
# We keep android/app/proguard-rules.pro tracked so the release rules persist.
android/.gradle/
android/app/build/
android/build/
android/local.properties
ios/

# Keystores and signing secrets — NEVER commit these.
*.keystore
*.jks
*.p12
keystore-base64.txt
android/app/release.keystore

# misc
*.log
.DS_Store
*.zip
